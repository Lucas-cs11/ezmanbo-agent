# 工作日报 — 2026-05-28

**项目**：ezplm-component-risk-agent  
**姓名**：陆杰（AI 技术负责人）  
**分支**：main（直接迭代，单人日）

---

## 今日完成

### 1. Merge `feature/backend/more-mock-data` → main

| 内容 | 说明 |
|------|------|
| `data/mock_parts.json` | 32 → **209 条**（新增 LDO、Boost、高压、车规等类型） |
| `ezplm查询接口操作手册/` | EZ-PLM API 操作手册 PDF/DOCX + 签名 Demo（JS/PHP/Python） |
| `scripts/add_boost_parts.py` | Boost 器件批量生成脚本 |
| `scripts/import_parts_from_api.py` | EZ-PLM API 批量导入脚本 |
| `analysis_report.md` | 新增分析报告文档 |
| main 侧 Python 文件 | 保留 main 版本（evidence/orchestrator/parser 今日改进优先） |

---

### 2. `app/requirement_parser.py` — mA 单位支持

| 改动 | 实现 | 测试句式 |
|------|------|----------|
| 新增 `CURRENT_MA_PAT` | `(\d+(?:\.\d+)?)\s*m[Aa]`，匹配后 ÷1000 转 A | `"200mA"` → `0.2A` |
| 电流提取优先级 | mA 优先，不命中再匹配 A | `"500mA"` → `0.5A` |

---

### 3. `data/mock_parts.json` — boost category 修正

| 问题 | 修复 |
|------|------|
| 6 条 Boost 器件 `category='boost'`（错误） | 统一改为 `category='dc_dc_converter'`（topology 字段保留 boost） |
| 修复前 Case 2（5V→12V 车规 QFN）返回 0 条候选 | 修复后命中 BOOST-国产-002，得分 88.33 |

---

### 4. `app/scoring.py` — 动态参数归一化

| 问题 | 修复 |
|------|------|
| `param_score / 3.0` 固定除以 3，LDO 等约束少时最高只得 33 分 | 新增 `param_checks` 计数器，按实际执行检查数归一化；无可检查项返回中性 50 |
| 修复前 LDO 候选全部得分 54.17（backup） | 修复后得分 77.5（recommended） |

---

### 5. `app/requirement_parser.py` — 电压比较兜底规则

| 场景 | 修复 |
|------|------|
| LLM 将 "5V转3.3V" 误标为 topology=ldo | 电压提取后：若 dc_dc_converter 且 Vin/Vout 均已知，按方向强制覆盖 topology（Vin>Vout→buck；Vin<Vout→boost） |
| LLM 模式 dc_dc_009 由 ❌ 变为 ✅ | — |

---

### 6. `tests/eval_runner.py` — LLM 端到端评测

| 改动 | 说明 |
|------|------|
| `load_dotenv()` 前置 | 必须在 `app.*` 导入前执行，因 llm_client.py 在模块级读取 env |
| `--llm` 模式 | 激活 OPENAI_API_KEY，使用 DeepSeek LLM 增强解析 |
| `--compare` 模式 | 同时跑纯规则与 LLM，输出逐 case 对比表 |
| 详细输出 | 每条：推荐数、TOP1 型号+得分、风险等级、字段级失败原因 |
| 结果文件 | `docs/eval_results.md`（规则）、`docs/eval_results_llm.md`（LLM） |

---

## 评测结果

```
────────── 纯规则模式 ──────────
dc_dc_001 ✅  dc_dc_002 ✅  dc_dc_003 ✅  dc_dc_004 ✅  dc_dc_005 ✅
dc_dc_006 ✅  dc_dc_007 ✅  dc_dc_008 ✅  dc_dc_009 ✅  dc_dc_010 ✅
通过率：10/10（100%）

────────── LLM 增强模式（DeepSeek）──────────
dc_dc_001 ✅  dc_dc_002 ✅  dc_dc_003 ✅  dc_dc_004 ✅  dc_dc_005 ✅
dc_dc_006 ✅  dc_dc_007 ✅  dc_dc_008 ✅  dc_dc_009 ✅  dc_dc_010 ✅
通过率：10/10（100%）— 电压比较兜底规则修复 dc_dc_009 LLM 误判
```

**LLM vs 规则差异**：dc_dc_007 LLM 推荐 137 条 vs 规则 7 条（LLM 未提取温度约束，候选更宽松，待后续分析）

---

## 今日提交记录

| 提交 Hash | 说明 |
|-----------|------|
| `b9ac48d` | merge(feature/backend/more-mock-data): 209 条 mock + API 手册 |
| `9f73b75` | fix: mA parsing, boost category, dynamic param normalization |
| `c5220cc` | feat(parser,eval): voltage-comparison topology guard + LLM eval mode |

---

## 遗留问题 / 明日待办

| 优先级 | 任务 | 文件 |
|--------|------|------|
| 高 | 论文技术部分初稿（评分维度设计、IR 结构、证据链方法论） | 论文文档 |
| 中 | dc_dc_007 LLM 宽松问题分析（温度约束未被 LLM 提取） | `app/llm_client.py` prompt |
| 中 | 补充 LDO 类测试用例（ldo_cases.jsonl） | `tests/cases/` |
| 低 | 替换 CODEOWNERS 中占位 GitHub 用户名 | `.github/CODEOWNERS` |

---

## 备注

- 运行命令：`PYTHONPATH=. python3 tests/eval_runner.py --compare`
- LLM 路径依赖 `.env` 中 `OPENAI_API_KEY`（DeepSeek v3）
- EZ-PLM API 手册已入库，后续 RAG 可直接使用 `ezplm查询接口操作手册/` 中的 PDF/DOCX
