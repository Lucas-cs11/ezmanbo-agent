# 工作日报 — 2026-05-28

**项目**：ezplm-component-risk-agent  
**姓名**：陆杰（AI 技术负责人）  
**分支**：main（直接迭代，单人日）

---

## 今日完成（上午）

### 1. Merge `feature/backend/more-mock-data` → main

| 内容 | 说明 |
|------|------|
| `data/mock_parts.json` | 32 → **209 条**（新增 LDO、Boost、高压、车规等类型） |
| `ezplm查询接口操作手册/` | EZ-PLM API 操作手册 PDF/DOCX + 签名 Demo（JS/PHP/Python） |
| `scripts/add_boost_parts.py` | Boost 器件批量生成脚本 |
| `scripts/import_parts_from_api.py` | EZ-PLM API 批量导入脚本 |
| `analysis_report.md` | 新增分析报告文档 |

### 2. `app/requirement_parser.py` — mA 单位支持

| 改动 | 实现 |
|------|------|
| 新增 `CURRENT_MA_PAT` | `(\d+(?:\.\d+)?)\s*m[Aa]`，匹配后 ÷1000 转 A |
| 电流提取优先级 | mA 优先，不命中再匹配 A |

### 3. `data/mock_parts.json` — boost category 修正

| 问题 | 修复 |
|------|------|
| 6 条 Boost 器件 `category='boost'`（错误） | 统一改为 `category='dc_dc_converter'` |

### 4. `app/scoring.py` — 动态参数归一化

| 问题 | 修复 |
|------|------|
| `param_score / 3.0` 固定除以 3，LDO 等约束少时最高只得 33 分 | 新增 `param_checks` 计数器，按实际检查数归一化 |

### 5. `app/requirement_parser.py` — 电压比较兜底规则

| 修复 |
|------|
| dc_dc_converter 且 Vin/Vout 均已知时，按方向强制覆盖 topology（Vin>Vout→buck；Vin<Vout→boost） |

### 6. `tests/eval_runner.py` — LLM 端到端评测

- `--llm` 模式 + `--compare` 模式，逐 case 对比表

---

## 今日完成（下午） 🔥 核心产出

### 7. 报告质量全面改进（评审对标修复）

| 模块 | 核心改进 |
|------|---------|
| **BOM** | Top 5 推荐（去重合并）、新增 Vout 列、"⚠待确认"缺失数据标记、RAG 知识参考节 |
| **风险评估** | 新增 4 条风险规则（数据缺失/地缘政治）、FMEA RPN 量化矩阵、5×5 热力矩阵、引用 IEC 60812/ISO 31000、修复编号 Bug |
| **拓扑分析** | 区分 Converter vs Controller 框图、新增效率与热估算节、PCB Layout 指导节（IPC-2221/2152）、含 RAG 上下文 |

### 8. P0 Bug 修复（6 项）

| Bug | 修复 |
|-----|------|
| 固定电压器件误推荐（LM2596-12 出现在 5V 需求） | `_infer_output_voltage_from_mpn()` + 输出电压匹配过滤 |
| API attrs "最大输出电压"覆盖 MPN 推断 | MPN 推断优先级 > attrs |
| "非车规"被解析为 automotive | 否定句式排除（非/不是/不要求/无需） |
| 温度 "-40 到 85°C" 解析为 Tmin=85 | 新增 `TEMP_RANGE_PAT` 范围模式 |
| 建议行动编号跳跃（1→3→5） | 动态计数器编号 |
| TPS54020 框图错误画外部 MOSFET（工程硬伤） | `_detect_converter_type()` 自动识别 |

### 9. RAG 工程知识库 — 最小可行版

**新增文件**：
- `app/rag.py` — ChromaDB + sentence-transformers（all-MiniLM-L6-v2）
- `data/knowledge/engineering_knowledge.json` — 12 条工程知识（Buck 设计/热设计/车规/Layout/供应链）
- `scripts/build_knowledge_base.py` — 构建脚本，支持 `--rebuild` `--query`

**集成**：`agent_orchestrator.analyze()` 自动查询 RAG，结果注入证据链。三份报告均展示 RAG 知识。

### 10. LangChain ReAct Agent

**新增文件**：
- `app/agent_tools.py` — 4 个 LangChain Tool
- `app/react_agent.py` — `create_agent()` + `run_agent()` + 多轮会话管理

**新增 API**：`POST /agent/chat`（`session_id` 多轮对话）

**验证**：单轮 2 次工具调用、多轮国产替代追问 4 次调用并正确记住上下文。递归限制 `recursion_limit=8`。

### 11. TopologyIR 结构化数据

**新增模型**（`app/schemas.py`）：`TopoNode` / `ExternalComponent` / `ThermalEstimate` / `TopologyIR`

**输出**：`docs/reports/.../04_TopologyIR.json`（7231B），含 nodes+components+thermal+mermaid+layout，对接 eZ-PLM IR 格式。

---

## 评测结果

```
纯规则模式 — 通过率: 10/10（100%）
dc_dc_001 ✅  dc_dc_002 ✅  dc_dc_003 ✅  dc_dc_004 ✅  dc_dc_005 ✅
dc_dc_006 ✅  dc_dc_007 ✅  dc_dc_008 ✅  dc_dc_009 ✅  dc_dc_010 ✅
```

---

## 项目规划差距跟踪（截至 5/28 晚）

| 规划项 | 原计划 | 状态 |
|--------|--------|------|
| IR 数据模型 (PartIR/RiskIR/TopologyIR) | W1 | ✅ 全部就绪 |
| eZ-PLM API 客户端 | W1 | ✅ HMAC 签名 + mock 兜底 |
| ChromaDB 向量知识库 | W2 | ✅ 12 条知识 + 检索集成 |
| LangChain ReAct Agent | W3 | ✅ 4 工具 + 多轮会话 |
| 场景 1 USB-C PD 方案演示 | W3 | 🟡 基础 Pipeline 跑通 |
| 场景 2 车规降压演示 | W3 | 🟡 跑通，mock 缺车规 5V 器件 |
| 场景 3 国产替代追问 | W3 | ✅ Agent 多轮对话可演示 |
| FastAPI + Streamlit 联调 | W4 | 🟡 后端就绪，前端待对接 Agent |
| 技术论文 | W4-5 | ⬜ 待开始 |
| 演示视频 / PPT / 展架 | W5 | ⬜ 待开始 |

---

## 明日工作计划（5/29）

| 优先级 | 任务 | 预计耗时 |
|--------|------|----------|
| 🔴 P0 | Streamlit 前端对接 Agent（展示工具调用 + 中间推理步骤） | 3h |
| 🔴 P0 | 论文技术部分初稿：RAG 知识增强方法论、评分维度设计、IR 结构、Agent 架构 | 4h |
| 🟡 P1 | 补充 mock 数据中车规固定 5V 器件（解决 dc_dc_001/008 0 推荐） | 1h |
| 🟡 P1 | RAG 对比实验：有 RAG vs 无 RAG 推荐质量量化差异 | 1.5h |
| 🟢 P2 | 补充 LDO 类测试用例 `ldo_cases.jsonl` | 0.5h |
| 🟢 P2 | Agent 幻觉缓解：API 无结果时禁止编造型号 | 0.5h |

---

## 备注

- Pipeline 评测：`EZPLM_API_KEY="" PYTHONPATH=. python3 tests/eval_runner.py`
- Agent 测试：启动 FastAPI → `POST /agent/chat {"user_input": "12V转5V 3A"}`
- RAG 构建：`PYTHONPATH=. python3 scripts/build_knowledge_base.py --rebuild`
- LLM 依赖 `.env` 中 `OPENAI_API_KEY`（DeepSeek v3）
