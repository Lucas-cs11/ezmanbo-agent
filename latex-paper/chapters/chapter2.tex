% ============================================================
% 第2章：方案论证与设计
% ============================================================

\section{总体架构设计}
系统采用“服务层—流水线推理层—工具层—数据层”的四层架构，将自然语言需求输入到三类工程文档输出的全过程组织为可自动执行的处理链路。各层之间职责边界清晰，数据流转方向限定为自上而下的逐层加工过程，从而提升系统的模块化程度、可维护性和可扩展性。

服务层基于 FastAPI 框架构建 API 服务，对外提供以下五个核心端点：\texttt{GET /health} 用于系统健康检查；\texttt{POST /analyze} 用于接收自然语言需求并返回完整的 \texttt{SelectionReport} JSON；\texttt{POST /replacement} 用于接收原始器件型号并返回 \texttt{ReplacementReport} JSON；\texttt{POST /agent/chat} 用于支持携带 \texttt{session\_id} 的多轮对话式选型交互；\texttt{GET /agent/sessions} 用于查看当前活跃的 Agent 会话列表。该层同时负责请求路由、参数校验、UTF-8 中文编码兼容和异常捕获，保障前端与推理层之间通信的稳定性。

流水线推理层实现了以下五阶段线性处理流程，将各阶段输出作为下一阶段输入，从而完成从用户意图到工程文档的端到端自动化转换。
\begin{itemize}
    \item 在需求解析阶段，自然语言文本经四级容错解析链转换为结构化约束对象。该过程首先由 LLM 完成语义映射，再由规则引擎通过关键词覆盖、电压方向校验和温度正则匹配逐级修正潜在错误;
    \item 在器件检索阶段，系统以结构化约束为输入，优先通过 HMAC-SHA256 签名认证调用 eZ-PLM 实时 API 执行多前缀分组查询，并使用五维过滤函数筛选满足约束的候选器件；当 API 不可用或返回空结果时，系统自动回退到离线器件数据库;
    \item 在混合评分阶段，系统对候选器件执行双模自适应评分：当检测到当前器件存在 eZ-PLM 关联参考设计时，启用四维增强评分模式；否则自动降级为纯规则双维评分模式。两种模式共享参数评分和供应链评分两个基础维度，仅在总分合并权重上有所差异，从而保证评分结果的可比性和可降级性;
    \item 在证据构建阶段，系统为每个候选器件生成 \texttt{EvidenceIR} 证据链条目，标注数据来源、计算置信度，并自动标记需要人工复核的异常项，例如库存为零、生命周期状态为停产或数据手册缺失等;
    \item 在报告生成阶段，评分结果与证据链被整合为 \texttt{SelectionReport}，并进一步生成 BOM 选型清单、供应链风险评估报告和电路拓扑分析报告三类工程文档，同时输出结构化 \texttt{TopologyIR} JSON 文件，以支持与 eZ-PLM 平台的数据互通。
\end{itemize}

工具层封装了六类可被上层调用的核心能力模块：eZ-PLM API 客户端、离线器件数据管理器、混合评分引擎、证据构建器、报告生成器和替代器件查找器。其中，eZ-PLM API 客户端负责 HMAC-SHA256 签名生成、请求发送和响应解析；离线器件数据管理器负责 JSON 格式器件库的加载与过滤；混合评分引擎集成规则评分与 LLM 软评分的双模切换逻辑；证据构建器负责八类字段级证据的自动生成；报告生成器分别面向 BOM、风险评估和拓扑分析三类文档；替代器件查找器则基于 \texttt{replacement\_for} 字段和同类拓扑交叉匹配，实现国产替代和功能等效替代搜索。上述模块均可独立测试和替换，有助于降低系统耦合度。

数据层汇聚了三类异构数据源。第一类为 eZ-PLM API，包括物料查询接口和参考设计查询接口。第二类为本地离线器件数据库，以 JSON 格式存储 1237 条器件记录，覆盖 Buck、Boost 和 LDO 三类拓扑，并包含国产与进口制造商、工业级与车规级等多维标签；当 API 不可用时，该数据库作为兜底数据源保障系统的独立运行能力。第三类为基于 ChromaDB 向量存储的 RAG 工程知识库，已收录 29 条工程知识条目，覆盖 Buck/Boost/LDO 设计公式、热管理、车规认证、PCB Layout 规范、供应链评估框架、EMI/EMC 设计、可靠性降额准则和 USB-C PD 协议等 11 个专业类别。知识条目通过 \texttt{sentence-transformers/all-MiniLM-L6-v2} 嵌入模型转换为 384 维向量，并在每次 Pipeline 分析过程中自动检索，检索结果随后被注入三类输出报告。

表\ref{tab:tech}汇总了系统核心技术栈的选型方案与对应的依据。
\begin{table}[htbp]
\centering
\caption{核心技术栈选型及依据}
\label{tab:tech}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{0.95}
\begin{tabularx}{\textwidth}{p{2cm}p{4cm}X}
\toprule
\textbf{类别} & \textbf{选用方案} & \textbf{选用依据} \\
\midrule
大语言模型 & DeepSeek & 支持高精度中文语义理解，API 调用成本可控，统一接口可切换至其他 OpenAI 兼容模型 \\
后端框架 & FastAPI 0.136 + Uvicorn & 异步高性能，自动生成 OpenAPI 文档，深度集成 Pydantic v2，支持类型安全校验 \\
数据建模 & Pydantic v2（2.13） & 声明式字段校验，支持 JSON 自动序列化，便于数据一致性验证 \\
向量数据库 & ChromaDB 1.5 & 支持持久化增量写入，余弦相似度检索满足工程知识语义匹配需求 \\
嵌入模型 & all-MiniLM-L6-v2 & 模型体积约 80\,MB，384 维向量兼顾检索速度与精度，适用于中英文混合语义检索 \\
Agent 框架 & LangChain 1.3 & 提供工具调用、会话记忆与多轮推理能力，ReAct 推理链可用 \\
前端展示 & Streamlit 1.24+ & 纯 Python 可视化交互界面，支持实时渲染评分仪表盘及证据链对比表 \\
\bottomrule
\end{tabularx}
\end{table}

\section{评分模型设计}
\subsection{双模自适应评分框架}

为兼顾评分结果的稳定性与智能化程度，系统设计了双模自适应评分框架。评分模式的选择取决于两个条件：一是 LLM 服务是否可用，二是候选器件是否具备 eZ-PLM 参考设计数据。仅当两项条件同时满足时，系统启用 LLM 增强模式；否则自动回退至纯规则模式。该机制既能够利用参考设计知识提升评分质量，又保证了在外部模型或数据不可用时系统仍具备稳定运行能力。

\subsection{纯规则模式}

当 LLM 不可用或缺少参考设计数据时，系统采用基于参数匹配和供应链稳定性的双维评分模型：

\begin{equation}
S_{\text{total}} = 0.80 \times S_{\text{param}} + 0.20 \times S_{\text{supply}}
\label{eq:rule}
\end{equation}

上述权重的设定依据了电子元器件选型领域的多属性决策实践。在元器件选型与管理的经典方法论中，电气参数的匹配程度始终被视作决定器件可用性的首要约束——若输入电压范围、输出电流能力或工作温度等级等硬性指标不满足设计要求，则该器件在工程上不可用，因此参数匹配维度的权重被赋予了主导地位（0.80）\cite{pecht2008parts}。供应链稳定性的权重（0.20）则参照了供应链风险评估领域中对库存水平与生命周期状态两大类指标的综合赋权研究\cite{chen2021supplyrisk,goel2021supply}，在参数匹配主导的前提下为供应风险的量化留出了合理的表达空间。

参数匹配分 $S_{\text{param}}$ 采用动态约束归一化方法计算：

\begin{equation}
S_{\text{param}} = \frac{\displaystyle\sum_{i=1}^{N} s_i}{N} \times 100
\label{eq:param}
\end{equation}

式中，$N$ 表示实际存在的参数约束项数量，$s_i \in [0,1]$ 表示对应约束项的归一化得分。当用户未提供明确参数约束时，系统将参数匹配分设为中性值 50，以避免对候选器件产生系统性惩罚。参数评分综合考虑了输入电压覆盖、输出电压匹配、输出电流满足和温度范围适配四个维度；供应链稳定性评分则依据库存水平和生命周期状态进行分段计算，并对停产或过时化器件施加显著惩罚，以充分反映供应链风险。

\subsection{LLM 增强模式}

当 LLM 服务可用且候选器件具备参考设计数据时，评分模型扩展为四维混合评分机制：

\begin{equation}
S_{\text{total}} = 0.40 S_{\text{param}} + 0.10 S_{\text{supply}} + 0.25 S_{\text{app}} + 0.25 S_{\text{design}}
\label{eq:hybrid}
\end{equation}

与纯规则模式相比，增强模式在权重分配上进行了两方面的调整。一方面，参数匹配的权重从 0.80 下调至 0.40，供应链稳定性的权重从 0.20 下调至 0.10，腾出的权重空间（合计 0.50）被分配给两个由 LLM 基于参考设计数据评估的软性维度——应用场景适配度 $S_{\text{app}}$ 和设计成熟度 $S_{\text{design}}$，各占 0.25。这一分配逻辑参考了智能选型系统中"硬约束主导、软知识增强"的分层评估思路\cite{wang2022selection}：当外部知识源（参考设计案例）可用时，系统有依据地将部分决策权重从确定性规则维度转移至知识增强维度；而当外部知识源缺失时，系统则自动回退至纯规则模式，以规则维度的更高权重保障评分结果的稳定性。两个 LLM 软评分维度的取值均被严格约束在 $[0,100]$ 区间，系统对 LLM 的返回 JSON 执行 clamp 校验与格式验证，任何异常均触发向纯规则模式的自动回退，从而确保增强模式的可靠性不会被 LLM 的不确定性所削弱。

\subsection{去重、排序与推荐分级}

eZ-PLM API 在返回同一型号族的查询结果时，常包含多个仅在封装后缀或卷带标识上存在差异的变体。以 TI 的 LM2596 系列为例，一次针对 Buck 拓扑的查询可能同时返回 LM2596S-5.0/NOPB（S 封装）、LM2596SX-5.0/NOPB（SX 封装）、LM2596T-5.0/LF03（T 封装）等同型号族的多条记录，这些变体在电气特性上完全一致，仅在物理封装形式和包装方式上有所不同。若不加处理地将其全部列入推荐列表，不仅会造成信息冗余，还会挤占其他独立型号的展示空间，降低推荐结果的有效区分度。

为此，系统通过 \texttt{\_extract\_core\_mpn()} 函数对每条候选器件的完整 MPN 字符串进行处理——去除 “/NOPB”、”/LF03” 等质量等级后缀、”-TR”、”-T\&R” 等卷带标识，以及末尾的封装变体字母（如 RUWT 中的 T），从中提取出代表该器件电气型号的核心 MPN（如 LM2596S-5.0）。以提取到的核心 MPN 为键，系统对全部候选器件进行分组去重：同一组内仅保留综合得分最高的那个变体，同时将被合并变体的完整型号信息追加至保留记录的 reasons 字段中，以确保变体信息的可查阅性。在上述 LM2596 系列的例子中，三个封装变体被合并为一条记录，最终推荐列表中仅展示该系列的最高分变体，从而将推荐结果的型号多样性从”同一系列的多个变体”提升为”不同系列的独立型号”。

完成去重后，系统按综合得分降序排列，并采用 Top-N 分级策略生成推荐结果。以一次典型的”12 V 转 5 V、3 A、工业级”选型查询为例，系统在检索阶段共召回 15 条候选器件，经去重合并后保留约 10 个独立型号，随后按评分排序并分级：综合得分不低于 75 分且排名前五的器件被标注为”推荐”（例如得分 93.33 的 TPS1100D 和得分 86.67 的 MOCK-BUCK-005）；排名第六至第十五且综合得分不低于 50 分的被标注为”备选”；其余器件则标注为”不推荐”。最终输出列表被截断至最多 15 条记录，兼顾了信息完整性与前端展示的可读性。

\section{数据模型设计}

为了同时满足 eZ-PLM 平台的数据互通需求与三类工程文档的结构化输出需求，系统以 Pydantic v2 为基础定义了一套标准化的中间表示（IR）数据模型体系，并为无 Pydantic 的轻量化部署环境提供了等价的 Python dataclass 兜底实现。该体系包含了五个核心数据模型。

\textbf{PartIR} 由 21 个字段组成，完整描述了一个电子元器件的全部可检索属性，各字段的类别归属与含义如表\ref{tab:partir}所示。其中，\texttt{source} 字段用于区分数据的真实来源（\texttt{ezplm} 或 \texttt{mock}），\texttt{output\_voltage\_v} 字段通过 MPN 型号推断函数与 API 属性解析结果的双源校验机制来避免固定输出电压被误识别。

\begin{table}[H]
\centering
\caption{PartIR 字段定义}
\label{tab:partir}
\small
\begin{tabular}{p{2.5cm}p{3.0cm}p{2.8cm}p{3.0cm}}
\toprule
\textbf{类别} & \textbf{字段名} & \textbf{类型} & \textbf{说明} \\
\midrule
\multirow{2}{*}{制造商信息} & part\_number & str & 制造商型号（MPN）\\
 & manufacturer & Optional[str] & 制造商名称 \\
\midrule
\multirow{4}{*}{电气参数} & input\_voltage\_min\_v & Optional[float] & 最小输入电压（V）\\
 & input\_voltage\_max\_v & Optional[float] & 最大输入电压（V）\\
 & output\_voltage\_v & Optional[float] & 固定输出电压（V），可调器件为 None\\
 & output\_current\_max\_a & Optional[float] & 最大输出电流（A）\\
\midrule
\multirow{2}{*}{物理属性} & package & Optional[str] & 封装类型（如 QFN-32、SOT-23-5）\\
 & temperature\_min\_c/max\_c & Optional[float] & 工作温度范围（°C）\\
\midrule
分类标识 & category / topology & Optional[str] & 器件类别（dc\_dc\_converter/ldo）与拓扑（buck/boost/ldo）\\
\midrule
国产标识 & is\_domestic & bool & 是否为国产器件\\
\midrule
\multirow{2}{*}{认证与合规} & automotive\_grade & bool & 是否通过 AEC-Q100 车规认证\\
 & lifecycle\_status & Optional[str] & 生命周期状态（active/nrnd/obsolete/discontinued）\\
\midrule
\multirow{3}{*}{供应链状态} & stock & Optional[int] & 当前库存数量\\
 & unit\_price\_cny & Optional[float] & 参考单价（人民币）\\
 & replacement\_for & List[str] & 可替代的型号列表\\
\midrule
\multirow{3}{*}{数据溯源} & datasheet\_url & Optional[str] & 数据手册链接\\
 & ezplm\_part\_id & Optional[str] & eZ-PLM 平台内部 ID\\
 & source & str & 数据来源标识（ezplm / mock）\\
\midrule
描述 & description & Optional[str] & 器件功能描述文本\\
\bottomrule
\end{tabular}
\end{table}

\textbf{ScoreBreakdown} 记录了一个候选器件在评分过程中产生的全部维度信息——各子维度分数（parameter\_match\_score、supply\_risk\_score、total\_score）、评分原因列表（reasons，包含参数覆盖情况、库存水平、LLM 评估结论等可读文本）、评分模式标识（scoring\_mode，取值为 rule\_only 或 llm\_enhanced）以及 LLM 子评分项的详细记录（llm\_application\_score、llm\_design\_risk\_score、llm\_reasoning），使得任何一条评分结论都能被追溯到其具体的计算依据。

\textbf{TopologyIR} 采用了四子模型嵌套结构，各子模型的字段定义如表\ref{tab:topoir}所示。顶层 TopologyIR 将下述三子模型与 Mermaid 框图源码、PCB Layout 规则列表和设计引用文件路径整合为一个可直接序列化为 JSON 的完整拓扑描述。

\begin{table}[H]
\centering
\caption{TopologyIR 子模型字段定义}
\label{tab:topoir}
\small
\begin{tabular}{p{2.8cm}p{2.8cm}p{2.4cm}p{3.5cm}}
\toprule
\textbf{子模型} & \textbf{字段名} & \textbf{类型} & \textbf{说明} \\
\midrule
\multirow{6}{*}{TopoNode（拓扑功能节点）} & node\_id & str & 节点唯一标识（如 vin\_cin、power\_ind）\\
 & node\_type & str & 节点类型（controller\_ic / passive\_inductor / diode / ground）\\
 & label & str & 中文标签（如"储能电感 L"）\\
 & description & str & 功能描述文本\\
 & connected\_to & List[str] & 邻接节点 ID 列表\\
 & signal\_type & str & 信号类型（power / control / feedback / ground）\\
\midrule
\multirow{8}{*}{ExternalComponent（外围元件）} & refdes & str & 参考位号（如 L1、CIN1、RFBT）\\
 & component\_type & str & 元件类型（inductor / capacitor\_mlcc / resistor / diode）\\
 & value & Optional[str] & 推荐参数值（如"6.8μH"、"22μF"）\\
 & tolerance & Optional[str] & 容差（如"±1\%"）\\
 & voltage\_rating & Optional[str] & 耐压等级（如"25V"）\\
 & temperature\_coeff. & Optional[str] & 温度系数（如"X7R"）\\
 & package & Optional[str] & 封装（如"0805"）\\
 & selection\_notes & Optional[str] & 选型备注\\
\midrule
\multirow{7}{*}{ThermalEstimate（热性能估算）} & estimated\_efficiency\_pct & float & 估算效率（\%）\\
 & output\_power\_w & float & 输出功率（W）\\
 & total\_power\_loss\_w & float & 总功耗（W）\\
 & junction\_temp\_c & float & 结温估算值（°C）\\
 & junction\_temp\_max\_c & float & 最高允许结温（°C）\\
 & thermal\_margin\_c & float & 热余量（°C）\\
 & needs\_heatsink & bool & 是否需要散热器\\
\bottomrule
\end{tabular}
\end{table}

\textbf{RiskIR} 包含整体风险等级（overall\_risk\_level，取值为 low、medium 或 high）和一个 RiskItem 列表。每条 RiskItem 记录了风险类型（supply 或 engineering）、严重程度、描述文本、相关器件型号、缓解建议以及证据引用字段（evidence\_refs）——后者为 FMEA 的 RPN（Risk Priority Number）量化扩展提供了数据接口。系统的规则引擎覆盖了 13 类确定性风险检测场景，所有检测结果均以 RiskItem 的形式被组织为结构化列表，LLM 仅能在此基础上进行工程背景的补充叙述而不能独立生成新的风险条目。

\textbf{SelectionReport} 与 \textbf{ReplacementReport} 分别作为 Pipeline 选型分析和替代器件搜索的顶层输出结构。SelectionReport 封装了原始用户输入、解析后的结构约束、候选器件完整列表、推荐器件子集、风险汇总、证据链全集和 Markdown 格式的摘要文本；ReplacementReport 则封装了原始型号信息、替代候选列表、兼容等级评估结论和结构化的对比摘要，为国产替代和功能等效替代两类场景提供了统一的数据返回格式。
